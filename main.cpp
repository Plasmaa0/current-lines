#include "Mesh.h"

int main(){
    auto m = Mesh();
    m.LoadFile("../test.msh");
    // load mesh
    // get base point
    // create current line generator
    // generate current line
    return 0;
}